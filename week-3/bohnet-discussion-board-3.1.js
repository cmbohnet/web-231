const header = require('../bohnet-header.js');
console.log(header.display('Christine', 'Bohnet', 'bohnet-discussion-board-3.1.js'))


const testArr = ['a', 'b', 'c', d];
//display all 
for (var x=0; x < testArr.length(); x++){
    console.log(`Item ${x} in testArr is ${testArr[x]}`)
}

